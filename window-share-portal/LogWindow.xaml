<Window x:Class="LogWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Window Share Portal - ログ"
        Width="900" Height="550"
        MinWidth="600" MinHeight="300"
        FontFamily="Segoe UI"
        FontSize="13"
        Background="#0f0f0f"
        Foreground="#c9d1d9">

    <Window.Resources>
        <!-- Scrollbar Thumb -->
        <Style x:Key="DarkScrollThumb" TargetType="Thumb">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border CornerRadius="3" Background="#3a3a3a" Margin="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkScrollPageButton" TargetType="RepeatButton">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="DarkVerticalScrollBar" TargetType="ScrollBar">
            <Grid Width="8">
                <Border Background="#0f0f0f" CornerRadius="4" Opacity="0.3"/>
                <Track x:Name="PART_Track" IsDirectionReversed="True">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource DarkScrollPageButton}" Command="ScrollBar.PageUpCommand"/>
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource DarkScrollThumb}"/>
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource DarkScrollPageButton}" Command="ScrollBar.PageDownCommand"/>
                    </Track.IncreaseRepeatButton>
                </Track>
            </Grid>
        </ControlTemplate>

        <Style TargetType="ScrollBar">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Width" Value="8"/>
                    <Setter Property="Height" Value="Auto"/>
                    <Setter Property="Template" Value="{StaticResource DarkVerticalScrollBar}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Button -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#21262d"/>
            <Setter Property="Foreground" Value="#c9d1d9"/>
            <Setter Property="BorderBrush" Value="#30363d"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#30363d"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1a1f27"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListBox Item -->
        <Style x:Key="DarkListBoxItem" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#c9d1d9"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1c2128"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1f3a5f"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Log Item Template -->
        <DataTemplate x:Key="LogItemTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="155"/>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="85"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding Time}"
                           FontFamily="Cascadia Mono, Consolas" FontSize="11.5" Foreground="#8b949e"/>
                <TextBlock Grid.Column="1" Text="{Binding Level}" FontSize="11.5"
                           Foreground="{Binding LevelColor}" FontWeight="SemiBold"/>
                <TextBlock Grid.Column="2" Text="{Binding Source}" FontSize="11.5" Foreground="#58a6ff"/>
                <TextBlock Grid.Column="3" Text="{Binding Message}" FontSize="11.5" Foreground="#c9d1d9"
                           TextTrimming="CharacterEllipsis"/>
            </Grid>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="16,12,16,0">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Content="選択コピー" Style="{StaticResource ModernButton}"
                        Padding="10,5" Click="OnCopySelectedLogs" Margin="0,0,6,0" FontSize="12"/>
                <Button Content="全件コピー" Style="{StaticResource ModernButton}"
                        Padding="10,5" Click="OnCopyAllLogs" Margin="0,0,6,0" FontSize="12"/>
                <Button x:Name="ClearLogsButton" Content="クリア"
                        Style="{StaticResource ModernButton}" Padding="10,5" FontSize="12"
                        Click="OnClearLogs"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="&#x1F4CB; 詳細ログ" Foreground="#e6edf3" FontSize="14"
                           FontWeight="SemiBold" VerticalAlignment="Center"/>
                <TextBlock x:Name="LogCountLabel" Foreground="#8b949e" FontSize="12"
                           VerticalAlignment="Center" Margin="10,0,0,0"/>
            </StackPanel>
        </DockPanel>

        <!-- Column Headers -->
        <Grid Grid.Row="1" Margin="26,8,26,3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="155"/>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="85"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="時刻" Foreground="#484f58" FontSize="11" FontWeight="SemiBold"/>
            <TextBlock Grid.Column="1" Text="レベル" Foreground="#484f58" FontSize="11" FontWeight="SemiBold"/>
            <TextBlock Grid.Column="2" Text="ソース" Foreground="#484f58" FontSize="11" FontWeight="SemiBold"/>
            <TextBlock Grid.Column="3" Text="メッセージ" Foreground="#484f58" FontSize="11" FontWeight="SemiBold"/>
        </Grid>

        <!-- Log List -->
        <ListBox x:Name="LogListBox" Grid.Row="2" Background="Transparent" BorderThickness="0"
                 Margin="16,0,16,12"
                 ItemTemplate="{StaticResource LogItemTemplate}"
                 ItemContainerStyle="{StaticResource DarkListBoxItem}"
                 SelectionMode="Extended"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 KeyDown="OnLogListKeyDown"/>
    </Grid>
</Window>
